<Window x:Class="FTPClient.ExplorerWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:local="clr-namespace:FTPClient"
		mc:Ignorable="d"
		Title="ExplorerWindow" Height="450" Width="800">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="0.5*"/>
			<RowDefinition Height="9.5*"/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="4*"/>
			<ColumnDefinition Width="0.5*"/>
			<ColumnDefinition Width="4*"/>
			<ColumnDefinition Width="1.5*"/>
		</Grid.ColumnDefinitions>
		<TextBox x:Name="HostPathTextBox" Grid.Column="0" Grid.Row="0" IsEnabled="False" Text="{Binding CurrentHostPath, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
		<TextBox x:Name="LocalPathTextBox" Grid.Column="2" Grid.Row="0" IsEnabled="False" Text="{Binding CurrentLocalPath, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
		<ListView x:Name="HostItemsListView" Grid.Column="0" Grid.Row="1"/>
		<Button x:Name="CopyButton" Grid.Column="1" Grid.Row="1" Height="30" Content="&lt;--&gt;" ToolTip="Скопировать элемент" Click="CopyButton_Click"/>
		<ListView x:Name="LocalItemsListView" Grid.Column="2" Grid.Row="1"/>
		<!--<ListView x:Name="LocalItemsListView" Grid.Column="2" Grid.Row="1">
			<ListViewItem Background="Cyan" Content="test	"/>
		</ListView>-->
		<StackPanel Grid.Column="3" Grid.Row="1" Orientation="Vertical" VerticalAlignment="Top">
			<Button x:Name="CreateWorkingDirectoryButton" Margin="0 0 0 10" Content="Создать каталог" ToolTip="Создать рабочий каталог"/>
			<Button x:Name="DeleteWorkingDirectoryButton" Margin="0 0 0 10" Content="Очистить каталог" ToolTip="Удалить рабочий каталог"/>
			<Button x:Name="StopButton" Margin="0 0 0 10" Content="Завершить работу" ToolTip="Завершить работу с сервером" Click="StopButton_Click"/>
		</StackPanel>
	</Grid>
</Window>
